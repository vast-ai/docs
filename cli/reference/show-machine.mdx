---
title: "vastai show machine"
sidebarTitle: "show machine"
---

[Host] Show hosted machines

## Usage

```bash
vastai show machine ID [OPTIONS]
```

## Arguments

<ParamField path="Machine" type="integer" required>
  id of machine to display
</ParamField>

## Options

<ParamField path="--quiet" type="boolean">
  only display numeric ids
</ParamField>

## Global Options

The following options are available for all commands:

| Option | Description |
| --- | --- |
| `--url URL` | Server REST API URL |
| `--retry N` | Retry limit |
| `--raw` | Output machine-readable JSON |
| `--explain` | Verbose explanation of API calls |
| `--api-key KEY` | API key (defaults to `~/.config/vastai/vast_api_key`) |
