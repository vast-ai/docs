---
title: "vastai tfa totp-setup"
sidebarTitle: "tfa totp-setup"
---

Set up TOTP-based 2FA using an Authenticator app.

## Usage

```bash
vastai tfa totp-setup
```

## Description

Generate a new TOTP (Time-based One-Time Password) secret for use with an authenticator app. The command displays:

- A QR code for scanning
- A manual entry key
- A secret token for the next activation step

### Workflow

1. Run `vastai tfa totp-setup`
2. Add the entry to your authenticator app by scanning the QR code or entering the key manually
3. Your app will show a 6-digit code
4. Complete setup with: `vastai tfa activate --method-type totp --secret <SECRET> <CODE>`

### Supported Apps

Any TOTP-compatible authenticator app will work, including:

- Google Authenticator
- Microsoft Authenticator
- Authy
- 1Password

## Examples

```bash
vastai tfa totp-setup
```

## Global Options

The following options are available for all commands:

| Option | Description |
| --- | --- |
| `--url URL` | Server REST API URL |
| `--retry N` | Retry limit |
| `--raw` | Output machine-readable JSON |
| `--explain` | Verbose explanation of API calls |
| `--api-key KEY` | API key (defaults to `~/.config/vastai/vast_api_key`) |
