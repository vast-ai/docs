---
title: "vastai accept price-increase"
sidebarTitle: "accept price-increase"
---

Accept a pending host price increase on one or more instances

## Usage

```bash
vastai accept price-increase ID [ID ...] | --host HOST_ID
```

## Arguments

<ParamField path="ids" type="integer" required>
  instance IDs to accept (one or more). Omit when --host is used.
</ParamField>

## Options

<ParamField path="--host" type="integer">
  accept every pending price-increase from this host (mutually exclusive with IDs)
</ParamField>

## Description

When a host raises the rental price, the auto-extend mechanism stops
renewing your affected contracts and an email notification is sent.
This command accepts the new price and lets the affected instances
keep auto-extending at the updated rate.

Batch accepts are capped at 64 instance IDs per call (server-side limit).

## Examples

```bash
vastai accept price-increase 123
    vastai accept price-increase 123 456 789
    vastai accept price-increase --host 12345
```

## Global Options

The following options are available for all commands:

| Option | Description |
| --- | --- |
| `--url URL` | Server REST API URL |
| `--retry N` | Retry limit |
| `--raw` | Output machine-readable JSON |
| `--explain` | Verbose explanation of API calls |
| `--api-key KEY` | API key (defaults to `~/.config/vastai/vast_api_key`) |
