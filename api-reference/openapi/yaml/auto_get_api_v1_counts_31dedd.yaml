openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v1/counts/:
    get:
      summary: Get Instance and Volume Counts
      tags:
      - Instances
      description: Returns the total number of instances and volumes for the authenticated user.
      security:
      - BearerAuth: []
      x-scope: instance_read
      x-rateLimit: 2
      responses:
        '200':
          description: '{"data": {"total_instances": <int>, "total_volumes": <int>}}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      total_instances:
                        type: integer
                        description: Number of active instances
                      total_volumes:
                        type: integer
                        description: Number of active volumes
              example:
                data:
                  total_instances: 3
                  total_volumes: 1
