openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/stripe-connect-disconnect/:
    get:
      summary: Disconnect Stripe Connect Account
      tags:
      - Billing
      description: Removes the authenticated user's Stripe Connect linkage and disables payouts.
      security: []
      x-rateLimit: 1
      responses:
        '200':
          description: 'Success; returns {"success": true}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true on success
                required:
                - success
              example:
                success: true
