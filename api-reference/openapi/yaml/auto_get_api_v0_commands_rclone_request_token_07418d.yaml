openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/commands/rclone/request/{token}/:
    get:
      summary: Get Rclone Request Script
      tags:
      - Instances
      description: Returns the rclone copy script for the host machine to execute, identified by the given copy token.
      security:
      - BearerAuth: []
      x-scope: instance_read
      x-rateLimit: 2
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token identifying the rclone copy request, embedded in the URL path.
      responses:
        '200':
          description: 'Returns {success: false, msg: "Invalid Token"} when no rclone request matches the token.'
