openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/instances/{id}/ssh/:
    get:
      summary: Get Instance SSH Keys
      tags:
      - Instances
      description: Returns the SSH keys currently associated with the specified instance.
      security:
      - BearerAuth: []
      x-scope: instance_read
      x-rateLimit: 2
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: Instance contract ID whose SSH keys to retrieve.
      responses:
        '200':
          description: 'Returns {success: false, error: "no_such_instance", msg: "Instance {id} not found."} when the instance
            is not found or not owned by the user.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: True on success
                  ssh_keys:
                    type: string
                    description: JSON-encoded array of SSH key objects
