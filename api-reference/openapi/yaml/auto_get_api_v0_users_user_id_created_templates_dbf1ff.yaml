openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/{user_id}/created-templates/:
    get:
      summary: List User Created Templates
      tags:
      - Accounts
      description: Returns all templates created by the specified user that have an active user-template relationship.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 1.6
      parameters:
      - name: user_id
        in: path
        required: true
        schema:
          type: integer
        description: ID of the user whose created templates to retrieve (URL path parameter)
      responses:
        '200':
          description: 'Returns {success: true, templates: [...]}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true
                  templates:
                    type: array
                    items:
                      type: object
                    description: Array of template objects created by the user
