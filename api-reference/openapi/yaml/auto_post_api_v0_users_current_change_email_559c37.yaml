openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/current/change-email/:
    post:
      summary: Change Email Address
      tags:
      - Accounts
      description: Send an email change verification link to the authenticated user's current address.
      security:
      - BearerAuth: []
      x-scope: user_write
      x-rateLimit: 10
      responses:
        '200':
          description: 'Verification email sent: {"sent": true}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  sent:
                    type: boolean
                    description: Always true when the verification email was dispatched
              example:
                sent: true
        '401':
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    Error:
      type: object
      properties:
        error:
          type: string
        msg:
          type: string
