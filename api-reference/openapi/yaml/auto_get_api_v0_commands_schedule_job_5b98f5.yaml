openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/commands/schedule_job/:
    get:
      summary: List Scheduled Jobs
      tags:
      - Instances
      description: Returns the list of scheduled jobs belonging to the authenticated user, up to 2048 results.
      security:
      - BearerAuth: []
      x-scope: instance_read
      x-rateLimit: 3.5
      responses:
        '200':
          description: Array of scheduled job records for the authenticated user
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
