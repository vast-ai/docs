openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/referral/{id}/:
    put:
      summary: Record Referral Visit
      tags:
      - Accounts
      description: Records a referral visit for the given referrer ID by logging the caller's IP address.
      security: []
      x-rateLimit: 2
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
        description: User ID of the referrer whose referral link was followed
      responses:
        '200':
          description: 'Referral recorded: {"success": true, "msg": "Referral Credited"}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true
                  msg:
                    type: string
                    description: Confirmation message
              example:
                success: true
                msg: Referral Credited
