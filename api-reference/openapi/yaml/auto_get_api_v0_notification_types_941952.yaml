openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/notification-types/:
    get:
      summary: List Notification Types Catalog
      tags:
      - Accounts
      description: Return the catalog of user-facing notification types for frontend discovery.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 2.0
      responses:
        '200':
          description: 'Success; returns {success: true, notification_types: list} where each entry includes key, slug, context,
            topic, category, display_name, and defaults'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true on success.
                  notification_types:
                    type: array
                    items:
                      type: object
                    description: List of notification type descriptors, each containing key, slug, context, topic, category,
                      display_name, and defaults fields.
