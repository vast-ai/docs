openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/cloud/:
    get:
      summary: List Cloud Connections
      tags:
      - Accounts
      description: Returns the authenticated user's linked cloud storage connections (Google Drive, S3, Backblaze, Dropbox).
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 2.9
      responses:
        '200':
          description: 'Successful response: list of cloud service connection objects with id, cloud_type, user_given_name,
            and other fields'
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  description: Cloud connection record with id, cloud_type, user_given_name, and other fields
