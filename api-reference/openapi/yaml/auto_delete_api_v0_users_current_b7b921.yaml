openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/current/:
    delete:
      summary: Log Out User
      tags:
      - Accounts
      description: Clear the current user session and authentication cookies.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit:
        profile: auth_burst
        threshold: 2
      responses:
        '200':
          description: 'Session cleared: {"sid": <server_id>}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  sid:
                    type: string
                    description: Server ID
              example:
                sid: '1'
