openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/cloud_integrations/:
    get:
      summary: List Cloud Integrations
      tags:
      - Accounts
      description: Returns the authenticated user's cloud storage integration names and types.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 2.9
      responses:
        '200':
          description: 'Successful response: list of objects with id, cloud_type, and name fields'
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                      description: Cloud connection ID
                    cloud_type:
                      type: string
                      description: Provider type (e.g. drive, s3, b2, dropbox)
                    name:
                      type: string
                      description: User-assigned name for this connection
