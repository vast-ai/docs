openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/ssh/:
    get:
      summary: List SSH Keys
      tags:
      - Accounts
      description: Returns all active SSH public keys associated with the authenticated user's account.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 1
      responses:
        '200':
          description: 'Successful response: list of SSH key objects'
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  description: SSH key record with id, user_id, public_key, and related fields
