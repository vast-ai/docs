openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/{user_id}/notification-prefs/:
    get:
      summary: Get Notification Preferences
      tags:
      - Accounts
      description: Returns the authenticated user's current notification preferences, or the default preferences if none have
        been set.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 2
      parameters:
      - name: user_id
        in: path
        required: true
        schema:
          type: integer
        description: Numeric ID of the user whose notification preferences are being retrieved.
      responses:
        '200':
          description: 'Success; returns {success: true, notification_preferences: object}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true on success.
                  notification_preferences:
                    type: object
                    description: Nested object keyed by context (e.g. 'host', 'client'), then by notification slug, then by
                      channel (e.g. 'email', 'webhooks').
              example:
                success: true
                notification_preferences:
                  host:
                    low_disk_space:
                      email: true
                      webhooks: false
                  client:
                    near_end_date:
                      email: true
                      webhooks: false
