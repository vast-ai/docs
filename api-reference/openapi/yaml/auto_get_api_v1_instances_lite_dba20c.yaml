openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v1/instances/lite/:
    get:
      summary: List Instances Lite (v1)
      tags:
      - Instances
      description: Lightweight paginated instance listing intended for the instances UI; currently disabled.
      security:
      - BearerAuth: []
      x-scope: instance_read
      x-rateLimit: 2.0
      responses:
        '404':
          description: Endpoint is currently disabled and always returns 404.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    Error:
      type: object
      properties:
        error:
          type: string
        msg:
          type: string
