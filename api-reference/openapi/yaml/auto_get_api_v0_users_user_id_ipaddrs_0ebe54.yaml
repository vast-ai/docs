openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/{user_id}/ipaddrs/:
    get:
      summary: Get User IP Addresses
      tags:
      - Accounts
      description: Returns the most recent IP addresses and user-agent records seen for the specified user.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 2.9
      parameters:
      - name: user_id
        in: path
        required: true
        schema:
          type: integer
        description: The ID of the user whose IP address history is being retrieved.
      responses:
        '200':
          description: 'Success: {"success": true, "results": [...up to 64 recent IP agent records...]}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true
                  results:
                    type: array
                    items:
                      type: object
                    description: Array of IP address records
