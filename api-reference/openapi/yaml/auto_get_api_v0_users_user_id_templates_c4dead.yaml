openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/users/{user_id}/templates/:
    get:
      summary: List User Templates
      tags:
      - Accounts
      description: Returns the active templates in the specified user's library, ordered by most recently updated.
      security:
      - BearerAuth: []
      x-scope: instance_read
      x-rateLimit: 1.6
      parameters:
      - name: user_id
        in: path
        required: true
        schema:
          type: integer
        description: ID of the user whose template library to retrieve (URL path parameter)
      responses:
        '200':
          description: 'Returns {success: true, templates: [...]}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true
                  templates:
                    type: array
                    items:
                      type: object
                    description: Array of template objects including user_template_id
