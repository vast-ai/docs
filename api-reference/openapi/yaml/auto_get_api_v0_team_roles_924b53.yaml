openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/team/roles/:
    get:
      summary: List Team Roles
      tags:
      - Team
      description: Returns the names of all active roles defined for the current team.
      security:
      - BearerAuth: []
      x-scope: team_read
      x-rateLimit: 2
      responses:
        '200':
          description: Array of role name strings
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
                description: List of role names defined for the team
              example:
              - team_manager
              - developer
              - viewer
