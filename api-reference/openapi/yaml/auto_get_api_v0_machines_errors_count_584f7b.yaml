openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/machines/errors/count:
    get:
      summary: Count Machine Errors
      tags:
      - Machines
      description: Returns the count of the authenticated host's active machines that currently have an error message set.
      security:
      - BearerAuth: []
      x-scope: machine_read
      x-rateLimit: 2.0
      responses:
        '200':
          description: 'Returns {count: <integer>} with number of machines reporting errors'
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
                    description: Number of active machines with at least one error message
              example:
                count: 3
