openapi: 3.1.0
info:
  title: Vast.ai API (auto-generated endpoint)
  description: Auto-generated from vast backend code introspection. Do not hand-edit — change web/ code or scripts/openapi_overrides.json
    and regenerate.
  version: 1.0.0
servers:
- url: https://console.vast.ai
  description: Production server
paths:
  /api/v0/secrets/:
    get:
      summary: List User Secrets
      tags:
      - Accounts
      description: Returns the authenticated user's environment variable secret keys with masked values.
      security:
      - BearerAuth: []
      x-scope: user_read
      x-rateLimit: 5
      responses:
        '200':
          description: 'Success: {"success": true, "secrets": {key: "********", ...}}'
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    description: Always true
                  secrets:
                    type: object
                    description: Map of secret key names to "********" (masked) or null
              example:
                success: true
                secrets:
                  MY_API_KEY: '********'
                  EMPTY_KEY: null
