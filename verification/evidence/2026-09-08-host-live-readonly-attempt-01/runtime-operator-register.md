# Runtime/operator prerequisites after the read-only batch

This is the runtime workstream only, not a declaration that all other repository work is blocked. See the [exact check-to-claim map](check-to-claim-map.json) and [current full runtime worklist](../../current-runtime-operator-blockers.md).

| What needs to be established | Current state and concrete prerequisite | Exact next action |
|---|---|---|
| Distinct client account and client-visible offers | **BLOCKED**: exact client-key location is unknown. No default CLI/key was found for the specified RTX4090 SSH user. Two key labels would not prove two accounts. | User supplies the precise key location or Keychain service/account, not the secret. Compare stable authenticated account IDs in memory and retain masked identity/offer observations. |
| Actual renter workload / self-test / rental lifecycle | **BLOCKED** for execution: no agreed paid-test budget, duration, exact workload/offer and cleanup authorization; client identity also unconfirmed. SSH/Host inventory are not proof of a rental. | Agree on the exact idle target, permitted workload, maximum spend/duration, monitoring/abort condition and instance cleanup. Confirm no conflicting workloads immediately before any approved run. |
| VM helper safety and status path, including VMs / Retry Enablement (`MCL-60ec9280a68ab6d7`) | **BLOCKED**: the default SSH user cannot read/traverse the exact installed helper path. Its check mode has not been independently established as safe. | Supply readable canonical source or authorize a narrowly scoped source read. Review safety before separately approving execution; no blind sudo/helper fallback. |
| Docker inventory correlation on the H100 | **BLOCKED**: default-user Docker socket read was denied in the earlier retained attempt. No inference about actual containers is permitted. | Provide an approved content-safe Docker metadata view or narrowly scoped read permission. Do not inspect renter files, environment variables or logs. |
| Nonempty maintenance / renter-report branches | **UNVALIDATED**: successful CLI/API responses were empty. Missing branch coverage alone is not a blocker. | Identify an existing approved, sanitized nonempty record/fixture; do not create maintenance or renter reports just to satisfy coverage. |
| GPU health under load, ECC error interpretation and quota enforcement | **UNVALIDATED**: enumeration, disabled/N/A ECC fields and visible `prjquota` are insufficient. | Specify suitable hardware and approved non-disruptive or isolated tests. Workload/privileged enforcement checks require separate scope before execution. |
| Windows and packaged Linux CLI coverage | **UNVALIDATED**: commands ran through canonical source entry point on macOS; Host metadata commands ran on Linux. | Use an approved runner/environment with the appropriate scoped credential; do not infer cross-platform results or publish CI secrets. |

Already available, not an external blocker: retained proof for all 15 Market Metrics CLI examples, raw Host machine inventory, storage/service snapshots and selected-machine/empty-response reads. Remaining command-level adjudication can use these records without repeating the live run. Most composite Host procedures still contain explicitly unexecuted steps.
