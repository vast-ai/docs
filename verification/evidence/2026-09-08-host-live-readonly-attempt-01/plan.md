# Host live read-only validation — attempt 01

Mode: PLAN_AND_EXECUTE. User requested as much outstanding Host Docs V&V as possible using the authorized Host. Baseline is [retry-02 baseline](../2026-09-08-host-rtx4090-readiness-attempt-02/baseline.json); subsequent additive planning/evidence records are present. Freeze each batch here before execution. No whole-page PASS inferred from a successful sample.

## Batch A — GPU Market Metrics / API

Exact current claim occurrences: CUR-a5b27de02fca3c9a (current snapshot), CUR-6c8062ab1c766957 (historical supply/demand/pricing), CUR-2de1188bec6c9f72 (GPU geography), all on `/host/market-metrics`, heading `API`. Each requires canonical implementation/source AND runtime observation. These literal endpoint rows describe the client-facing API; the prose itself is not proof.

Independent implementation basis: `vast-ai/vast-cli`, HEAD `18c4f2ccd6da587d5352f8741c71805a9a18e1ae`, `vastai/api/metrics.py` (gpu_current, gpu_history, gpu_locations), `vastai/cli/commands/metrics.py` and `vastai/api/client.py`; capture source digests and per-path dirty status. Repository OpenAPI schemas `metrics_gpu.yaml`, `metrics_gpu_trends.yaml`, `metrics_gpu_locations.yaml` supply the request/response contract and must be separately hashed. Client dispatch is not server-internal semantic proof.

Using only the exact user-designated Host Keychain entry in memory, perform these three HTTPS GET requests to `https://console.vast.ai` with Bearer authorization, no query credentials, no redirects, 20-second timeout, and at least 3 seconds between starts:

1. `/api/v0/metrics/gpu/current/?verified=yes&hosting_type=secure_cloud` — same HTTP request parameters as the documented curl example. Expect JSON success and a GPU array with supply/demand/pricing fields. Retain a bounded aggregate RTX4090 sample and field shapes, not all marketplace records.
2. `/api/v0/metrics/gpu/history/?gpu_name=RTX%204090&verified=all&hosting_type=all&step=3600` — representative requested GPU, default date window, hourly step. Expect supply/demand and pricing time series. Retain structural lengths and bounded first samples, and explicitly state this does not establish universal cadence/history guarantees.
3. `/api/v0/metrics/gpu/locations/` — expect location records and GPU metadata. Retain row count, safe field names/type/count checks only; do not persist precise coordinates, city, raw addresses or individual Host identities.

For each request retain method, non-secret URL, timing, HTTP status, full-body digest and privacy-filtered projections with their selection rule. Never retain headers, raw account/key data or full geography payloads. PASS is bounded endpoint availability/response-shape validation with the inspected client/schema implementation basis. It is not a completed CLI command run, Finance/pricing-policy approval, guaranteed semantic accuracy of market measurements, non-Host account access rule, or complete filter validation. Record missing fields/shape mismatch as FAIL at the tested contract level; credential, rate-limit, network or environment failure has a concrete prerequisite and separate status. Preserve all failures and add retests, not silent fixes.

## Remaining batches

### Batch D — official CLI source-entry-point reads, isolated local config

Use the existing official CLI checkout's `.venv` interpreter (xdg 6.0.0, requests 2.34.2) with that checkout on PYTHONPATH and `vastai.cli.main:main`, the console entry point declared in its pyproject. This validates source-entry-point command behavior on macOS, not installation on the Linux Host or released-package identity. Capture exact source HEAD and file digests; no source edits. Disable bytecode and debug output. Set separate temporary XDG config/cache paths with a non-secret placeholder key file to prevent copying the user's legacy key; HOME and normal CLI config remain untouched. Retrieve only the approved Host Keychain key in memory and supply it through the child environment, never argv, logs or files.

Unlike direct API batches A/C, the unmodified official CLI transport includes its legacy query authentication as well as the Bearer header. It is sent only by the canonical read-only commands to the official Vast server. Do not use explain/curl modes or retain credential-bearing URLs, raw account responses, key suffixes or headers. This authenticates the source CLI's actual behavior rather than a modified/mock transport. Key and suffix redaction is applied before output; sanitize private machine identifiers and geography. Any HTTP/CLI error is retained and not treated as PASS merely because the process exits zero.

Frozen commands: `show machines --raw`; `show machines`; `show machine <selected-machine> --raw`; `show maints --ids <selected-machine> --raw`; `reports <selected-machine>`; `metrics gpu`; `metrics gpu --verified true --datacenter true`; `metrics gpu --raw`; `metrics gpu-trends "RTX 4090"`; `metrics gpu-trends "RTX 4090" --raw`; `metrics gpu-locations --gpu "RTX 4090, H100_SXM" --raw`; `metrics gpu-locations --raw`. Prefix each with the normal Vast CLI entry point. Keep at least 3 seconds between runs; 30-second per-process timeout. No client/renter key assumption, mutations or workload actions.

Map these to the exact claim occurrences from Batch A/C and the current Market Metrics CLI code blocks, preserving composite/untested-command limits. Retain command arguments with target alias, exit code, timestamps, output digests, and privacy-safe selected output/shape. Locations retain field names/types/counts only, not coordinates/cities. Text inventory retains header and selected-machine row with identifiers masked. Empty results cannot prove nonempty rendering. Retain temporary directory creation/cleanup status; do not delete anything outside the exact task-owned directory.

### Batch C — selected-machine Host API reads and VM-helper safety inspection

Before executing any VM command, inspect only the exact installed `/var/lib/vastai_kaalia/enable_vms.py` source for readability, SHA-256 and the code path of `check`; do not import it or execute it merely because the documentation labels it a check. A missing permission is an explicit input/access prerequisite. This inspection does not prove upstream source provenance or VM runtime behavior. No broad filesystem search.

Use the same Host credential for GET `/api/v0/machines/<selected-machine>/?owner=me`, GET `/api/v0/machines/maintenances/?owner=me&machine_ids=[<selected-machine>]`, and GET `/api/v0/machines/<selected-machine>/reports/` (the latter request shape follows the repository OpenAPI path parameter; client code additionally sends machine_id as a GET JSON body, so a body-free schema request does not prove that exact CLI transport). Independent client basis: `vastai/api/machines.py` show_machine/show_maints/reports at the inspected CLI revision; reports contract in `api-reference/openapi/yaml/reports.yaml`.

Candidate claims: MCL-67c1400801647f68/MCL-f0b9b724554ce68a and related Fleet Operations inventory commands; MCL-0405647139c13da7 (Maintenance Windows / View Scheduled Maintenance); MCL-95331d12bb886909 (Workload Policy / user reports). These API checks provide related runtime context, not exact CLI execution or proof of follow-up diagnosis. Retain only response success/shape/count and selected-machine hardware/state, source/body digests and safe field names. Do not retain report free text/problem text, raw scheduled dates, account IDs, addresses, SSH details or renter data. Empty report/schedule results do not validate nonempty rendering or troubleshooting branches. Same-origin slash redirects may be diagnosed without credentials and corrected only in a new recorded attempt.

Default-user Host inventory/service/storage checks are being mapped to exact current claims before execution. Client-key checks wait for an exact authorized location: the RTX4090 default environment has no CLI or standard key file. A client role or distinct account cannot be assumed from a key label. No other credentials will be searched.

No sudo, Host software install/configuration, log or renter-content inspection, network/port probes, paid rental, self-test, workload execution, deletion, VM mutation, contract acceptance, external post or push is authorized by this plan. Paid and state-changing work needs exact scope/limits/cleanup authority separately.
