# Calculator evidence wording

Scope: explain the pending link check for MCL-18f04c2ae7ee95b4,
Earnings & Pricing Model / Market Data, host/earning.mdx:103.
Presentation only. No external validation, claim-status change, Host operation,
publication or human acceptance is requested or recorded.

1. Record the exact working tree, index, HEAD and file hashes before editing.
2. Clarify the link-check completion criteria in the shared reader. Keep checks
   of calculator arithmetic and market-data provenance separate and conditional.
3. Verify exact-match guards, unchanged claim/evidence records, and matching
   HTML and localhost presentations. Refresh the generated HTML and local proxy.
4. Retain the checks and limitations, update traceability and the AST graph.

Success: a reviewer can identify the exact link, the observation to retain, and
what that observation does and does not establish. Other entries and all claim
statuses stay unchanged. Current evidence remains available without being
relabeled as an external observation.

Baseline: baseline.json contains the pre-edit tracked/untracked file hashes,
Git porcelain-v2 state, index hash, branch and HEAD. The first buffered diff
capture exceeded Node's default output buffer (ENOBUFS). It wrote the complete
baseline.json but no patch. A streamed retry retained working-tree.patch and
index.patch before edits. The original baseline was not replaced.
