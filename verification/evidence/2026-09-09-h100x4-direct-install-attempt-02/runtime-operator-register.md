# Runtime/operator work — remaining after this install

The previous sudo-authentication blocker is **resolved by retest**, not erased. See [preflight](preflight-01.json), [post-install observation](postcheck-02.json) and [settled observation](settled-01.json). Core services, GPU visibility and project quota are observed on this host only. The latter snapshot has no Docker containers or GPU compute PIDs; no assistant cleanup command or reboot was necessary.

| Remaining item | Current disposition and claim impact | Exact prerequisite / next action |
| --- | --- | --- |
| Publish the approved machine offer and verify terms | **BLOCKED — missing bandwidth-price and prepaid-discount choice.** Machine 150296 is owned and unlisted in the retained API read. No offer-write request sent. | Authorized listing owner confirms upload/download $/GB and maximum prepaid discount. Proposed: both $0/GB and discount disabled. Then refresh exact identity/occupancy, publish only approved fields, fixed expiry 1789423200 and `vol_size=0`, and independently read back all terms. Do not extend expiry. |
| Separate marketplace self-test / representative rented workload | **BLOCKED — no approved paid test budget, bounds or external test location for this new host.** Embedded NVML/NCCL success is not that workflow. | Operator supplies explicit target, spend ceiling, duration, external/WAN runner where needed, test/image version and cleanup authority; recheck occupancy immediately before running. Do not borrow approval from previous hosts or rentals. |
| Reboot persistence / final acceptance | **BLOCKED — explicit no-reboot constraint remains.** Active/enabled units and persistent mount configuration are observations, not a successful reboot. | Operator approves a separate no-customer maintenance window and acceptance plan, then retains pre/post boot identity, GPU/service/storage/network state. Human acceptance must be recorded by the authorized reviewer. |
| Stock guided-TUI / unchanged standard-installer paths | **UNVALIDATED — not run by this attempt.** The locally modified direct route omits automatic self-test/listing and cannot prove the stock path. | Agree on the actual shipped source/binary and a controlled clean host, safe listing settings, valid setup credential and permitted diagnostic actions. Retain full TUI flow or unchanged standard execution in a separate attempt. Do not reinstall this live daemon just to collect evidence. |
| Public-self/NAT reflection and externally forwarded-port behavior | **UNVALIDATED in this attempt.** The observed `30000-30499` file is not proof of WAN reachability or router reflection. The previously reported reflection issue is not claimed fixed. | Bind the earlier exact operations evidence, agree on external test endpoints and any needed network-change authority, then retain before/after container connectivity. No router/firewall change was made here. |
| Default public-image preload | Original **FAIL**, then bounded retry **PASS** in [image-retest-01.json](image-retest-01.json). Output retains `pytorch/pytorch@sha256:11691e035a3651d25a87116b4f6adc113a27a29d8f5a6a583f8569e0ee5ff897`. | No further image action needed for this retry. Do not mark the original pull successful or infer a container workload from an image download. Error propagation remains source-owner work. |

Installer updater-permission and package-lock errors are in the separate [source-owner register](source-owner-register.md). Current repository claim/interface tests are not runtime/operator acceptance.
