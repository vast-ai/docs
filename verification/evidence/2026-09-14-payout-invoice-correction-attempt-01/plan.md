# Payout and invoice wording: plan and execute

Scope: CON-1518 / PR185, current dirty tree captured in baseline.json before
content changes. Four FAIL occurrences in host/payment.mdx: MCL-e2b956d14494e470,
MCL-df7b287adb0df683, MCL-5936430d1b2d8de9 and MCL-bbd64c772e9b4693.
Inspect repeated timing under "When will I get paid?" so the same unsupported
clock time is not left elsewhere. Inventory is payment-inventory.json.

1. Inspect current official public payout guidance, retaining exact headings,
   URL, retrieval time, wording, hash and publication provenance. The current
   draft is the claim under review, not its proof. The published page shares
   this docs repository: it can establish what Vast publishes as guidance,
   not independently validate backend scheduling or payment execution.
2. Distinguish invoice threshold from payout threshold; inspect Friday timing,
   clock-time qualifications, rollover and account prerequisites separately.
   Correct only wording supported by the selected authority. Record any gap.
3. Bind exact citations and excerpts to the corresponding corrected passages.
   Keep unchanged claims unchanged; preserve all earlier evidence. Version the
   correction above the frozen current projection and mark affected procedures
   stale instead of inheriting execution proof.
4. Retest generated model, source bindings, provenance guards, historical
   preservation, links and claim highlights. Check all Payment locators and the
   corrected cards/source controls in both offline HTML and localhost4000.
5. Refresh traceability and the correction walkthrough with exact remaining
   counts and limits. No human acceptance or general payment validation.

Methods: static source comparison and safe local/browser checks on macOS.
An independent source reader challenges the interpretation; a separate writer
implements the coupled model/reviewer change. The main agent verifies it.

PASS means the exact revised statement is supported within its recorded source
scope. Missing evidence alone is UNVALIDATED. Confirmed wording/citation defects
are FAIL. BLOCKED requires a specific unavailable prerequisite. Unsupported
details must not gain PASS from a nearby source or a passing local test.

No account, credential, payment, rental, Host, reboot, financial operation,
push, merge or publication. Public retrieval needs network access only.

## Source-review scope amendment, 14 September 2026

The fresh published capture (published-invoice-guidance-01.json) states an
invoice-generation threshold, Friday generation without a clock time, and a
first-payout estimate with provider processing qualifications. The draft repeats
the unsupported noon-Pacific and 2-to-4-week wording. Add MCL-3d796f5ae7f2020e
(Payout Schedule, line55) and MCL-3afd93ae0b6cf8a4 (When will I get paid?, line84)
to the correction inventory. They are currently UNVALIDATED, not four more FAILs.
Six exact records are now in scope; the remaining 2,007 must remain unchanged.
Keep the existing independently retained Hosting Agreement citation for its
separate contractual clock. Do not claim either a guaranteed payment time or
completed payment execution. The current view should say "Published guidance
checked" and make this evidence limit explicit.

## Reviewer classification correction found during rendered verification

The current work queue does not recognize four newer classes. It labels all
11 supported Payment findings "Needs triage", including the six invoice items,
three previously checked provider options, the Terms summary and bank-transfer
FAQ. Register those four exact categories in the display taxonomy and retain
the unknown-category fallback for genuinely unknown records. This changes only
queue labels/counts, not claim statuses or evidence. The six-record model-change
boundary and the 2,007 unchanged claim records remain mandatory.
