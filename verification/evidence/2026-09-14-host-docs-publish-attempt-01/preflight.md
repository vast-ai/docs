# Publication preflight

Target: existing draft [vast-ai/docs PR185](https://github.com/vast-ai/docs/pull/185),
fork jjziets/docs, branch CON-1584-host-cli-api-sdk, base main. The fetched fork
head bfa926c is an ancestor of local 4fa6fbb; the local branch is one commit ahead,
not diverged. That earlier progress commit is also included by a normal push.
Its separate September 9 publication package retains an exact named-credential
absence check on 1,934 files and an independent pattern/OCR review. Those are
dated, bounded observations, not a new exhaustive privacy certification:
[earlier publication result](../2026-09-09-host-progress-publication-attempt-01/result.md).

The pre-staging inventory has 1,749 accumulated Host Docs task files, about
523 MB, across source pages, reviewer implementation, current projections and
14 evidence-attempt directories. No unrelated application file or symlink was
identified. No file exceeds 100 MiB. The 85.6 MB HTML and 80.7 MB historical patch
are large, retained artifacts; publishing them is not a claim that GitHub can
render the interactive HTML. Download the raw HTML to review it offline.

The current invoice correction depends on the prior payout Terms, provider,
cleanup, jurisdiction, Terms, clarification and authority layers. They are
published together. The active reader used 408 repository files; all are in the
tracked/candidate package. Scoped reference inspection found all 443 repository
references and all 294 path/hash pairs present/matching. Independent reader
reports are supporting audit observations, not replacements for root tests.

Root checks retained here: 93 Python tests (python-01.json), 68 JavaScript tests
(reviewer-01.json), deterministic export (export-check-02.json), and source/code
whitespace (source-whitespace-01.json). The publication warning originally said
the report had not been pushed. The new expectation fails on that warning in
publication-wording-before-01.json; after replacing it with commit/digest
comparison guidance, all 35 HTML tests pass in publication-wording-retest-01.json.
These 35 are a focused retest, not 35 additional unique tests beyond the 68.

The current claim model is unchanged by publication preparation:
SHA-256 56650fad892d1f1d387dd4328bdb474f7c2c923907419a2b74868c4a68b7daf5.
It still records 319 scoped PASS, 26 FAIL, 23 BLOCKED, 87 N/A and 1,558 UNVALIDATED
on 44 Host pages, with 18 CLI and 15 SDK support references separate. No remaining
external work is declared complete. The V&V source review does not prove a
successful invoice or payment. No new product/Host/account/paid operation ran.

The AST-only graph refresh completed; graph/HTML-size and zero-node warnings are
retained in graph-update-01.json. Ignored graph files are not commit payloads.
The independent privacy audit covered all 1,749 frozen files and 190 archive
entries, including OCR on 55 unique images (88 working-tree/archive occurrences).
It found no confirmed account credential or private payment-data blocker. Public
page examples and logged-out frontend metadata are distinguished from credentials
in [the compact disposition](privacy-disposition.json). The main agent inspected
its categories, source contexts and scope and visually checked selected payout
and volume reviewer images. Four later template/test/export changes contain only
the retained publication-copy correction; the current model is unchanged. The
new publication records contain paths, hashes, test results and authored handoff
text, not new account observations or credential captures.

This is a bounded heuristic/advisory audit, not exhaustive privacy certification.
Internal historical Host and local preview addresses remain as operational
metadata. The compact report retains hashes of temporary scanner/OCR outputs;
those temporary reproduction paths are not shipped as a fresh-clone toolchain.

Archives preserve implementation history and earlier corrected review findings;
they are not all active reviewer inputs. In particular the invoice extra archive
contains otherwise absent guard-browser evidence, including the withdrawal of an
overstated worker result. No historical evidence is silently deleted.

Publication outcome is recorded by the Git commits, matching remote PR head and
the subsequent publication receipt. This preflight alone is not proof of a push,
merge, CI result or human acceptance.
