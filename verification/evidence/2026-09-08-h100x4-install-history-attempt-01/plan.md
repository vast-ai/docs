# H100 onboarding readiness and prior installation evidence intake

Mode: PLAN_AND_EXECUTE for bounded read-only capture; audit existing installation records. No installer or workload execution is authorized by this plan. Basis: the user's supplied new four-GPU target, latest operations runbook/rebuild record and request to recover the two earlier installations.

## Inventory before execution

1. HIST-01: Preserve dirty repository/index state before edits. Inspect the user-designated operations/rebuild records, retaining digests and sanitized observations. Resolve current identities and access paths; superseded routes must not be used.
2. HIST-02: Read-only SSH baseline for the newly designated four-GPU host: current time, OS/kernel, GPU count, storage mounts, service/install presence, readable helper/installer identities. Expect either target-consistent observations or a concrete access failure; do not claim workload health from inventory. Use existing trusted SSH host keys; do not bypass host-key verification.
3. HIST-03: Resolve the two prior standard-install targets from the current runbook; inspect relevant retained installer/package/setup records and narrowly filtered setup history, with secrets redacted before tool output. Do not dump complete history or read unrelated workloads. A historical command line is invocation context, not proof of its exit or success. Capture source digests/timestamps and applicability limits; missing detail stays UNVALIDATED.
4. HIST-04: Independently inspect the public installer source without executing it, particularly registration/listing/self-test side effects and provenance. Public artifact download is read-only network retrieval; no API account/credential or Host mutation. Distinguish observed source from confirmed released lineage.
5. HIST-05: Bind only claim-suitable evidence; preserve prior passes/failures. Produce a sanitized reviewer-facing result and an exact remaining-action list. Any projection correction needs a new retained retest; update port4000/HTML only from supported current records, not by promoting history to runtime proof.

Safety: no installation, package/driver/disk/firewall/NAT change, reboot, GPU load, public-self probe, listing/unlisting, instance/rental operation, BMC/Redfish login or power action, VPN configuration change, history deletion, credential redemption, push, merge, external post or acceptance. A setup credential pasted into chat will not be reused or retained; a fresh credential and explicit automatic-listing/workload boundary are prerequisites for any future installer run. Preserve older evidence unchanged. Raw histories/logs and infrastructure identifiers remain restricted; publish only reviewed sanitized derivatives.

PASS applies only to an observed bounded read/inspection or a sufficiently supported exact claim. Confirmed mismatch is FAIL; suitable check prevented by specific access/input/authority is BLOCKED; absent or incomplete proof is UNVALIDATED. This package cannot imply that the new host is installed, rentable, accepted or healthy under load.

## Amendment 1 — 2026-09-08, before expanded capture

Recorded by the assistant from the user's clarification. HIST-03 now covers all three existing hosts: H100×8, RTX 4090 and RTX 6000 Ada. Only host installation records and narrowly filtered setup history are in scope. Do not inspect renter data or containers; do not reboot any host. Preserve permissions/access failures instead of escalating automatically. Each capture must retain target alias, source digest, timestamp, exact read method, sanitizer limits and observation; history cannot recover an unrecorded historical exit.

## Amendment 2 — 2026-09-08, conditional listing authorization

The user explicitly permits listing the new H100×4 and confirmed USD $3 per GPU-hour on-demand, $0.30 per GPU-hour minimum bid and $0.50 per GB-month storage, ending no later than September 15, 2026. This supersedes only the earlier blanket listing prohibition, subject to these constraints. Do not publish at generated/default prices even temporarily. Before any mutating execution, establish the authoritative API/installer unit conversion, fixed expiration timestamp, safe no-reboot route and initial-publication control. Record an execution subplan before running it. A fresh setup credential delivered securely remains required; the credential pasted into chat will not be used or retained. No authorization to reboot, affect existing workloads, alter networking, accept customers on another host, or bypass these pricing constraints is inferred. Source inspection and read-only history capture can continue while these prerequisites are unresolved.

## Amendment 3 — additive reviewer presentation, before implementation

HIST-05 will show the new pre-install inventory and public-helper source findings alongside exact current installation-page passages in the localhost reviewer and standalone HTML. This is supplemental evidence intake, not adjudication. Keep all current claim statuses, counts, history and exact-source gates unchanged. Bind only the specific page/heading/text and selected capture, reject source/hash drift and arbitrary artifact paths, retain the original failed SSH attempt and source-inspection correction. The initial real-browser baseline located all 51 claims on the installation page. After integration, run focused positive/negative tests, export consistency, source/status preservation checks and a new affected-page browser/evidence-link retest. Browser actions are limited to local review controls, never Host-console actions or executing documentation commands.
