# Published Terms citation and source binding

PLAN_AND_EXECUTE, 10 September 2026. The user authorized applying the Terms
source check discussed in the preceding read-only review. This is a bounded
documentation/source correction, not a new policy decision or live Host test.

## Inventory and source

Start from the exact dirty working tree in baseline-01.json and the frozen
before-review.json. Scope: Workload Policy, primarily Restricted Activity.
Initial targets are MCL-99ca28f707d5966a (introductory Terms assertion),
MCL-c4c4bfc59eb7b49f (malware/spam), MCL-633317ca7ebecfef (export restrictions),
and MCL-fe3eccd1cd40b4bd (mining). Inspect adjacent policy rows for the same
missing source, and record any bounded inventory amendment before changing them.
Account conversion, listing behavior, payout-provider liability and runtime
checks remain outside this correction.

The source is the official https://vast.ai/terms publication. Retain capture
time, version date, canonical URL, response/body digests, exact section/item
locators and bounded excerpts or summaries. Check clause context and applicable
scope independently. Existing Hosting Agreement evidence stays intact.

## Method and expected result

Inventory amendment, before source edits: include adjacent rows
MCL-2c3f7082e2c7fdf2 (illegal/abusive activity) and MCL-399798a3c4946b5f
(intellectual property and renter-data privacy). This makes six target claims.
The retained Terms can support the prohibited-use rules; existing independent
Hosting Agreement capture supports the Provider-specific privacy restriction.
Remove unsupported escalation wording from these rows rather than treating a
related prohibition as proof of a mandatory escalation workflow. Keep the
already-supported platform-abuse row unchanged.

1. Retain the pre-edit inventory and original source failures.
2. Capture the published Terms without credentials. Compare each target's exact
   wording to the relevant clause. A published rule can support that rule; it
   does not establish runtime enforcement or an unstated escalation procedure.
3. Add precise section/item citations, narrow wording where necessary, and bind
   independent source evidence only to what it supports. Preserve a concrete
   remaining action for any unsupported part. Keep the credit-card condition on
   the mining restriction; do not infer a blanket ban or permission elsewhere.
4. Project an explicit, hash-bound correction after the existing validated model.
   Preserve all unrelated claims/evidence, source history and support layers.
   Relocate exact unchanged passages; mark affected old procedure spans STALE
   when the changed customer text no longer supports their prior mapping.
5. Regenerate and check the model, worklist, separate runtime/source registers,
   shareable HTML and current localhost reviewer. Retest source drift, citation
   and cross-claim binding guards, exact passage controls and displayed limits.

PASS is permitted only for the precise cited policy assertion or bounded static
check. Missing evidence remains UNVALIDATED; a required missing citation remains
FAIL; BLOCKED requires a specific unavailable prerequisite. No whole-page or
runtime PASS follows from a Terms citation. Documentation, registry and graph
records are not independent proof of the policy being reviewed.

## Safety and handoff

Only public-source retrieval and repository/local-review operations are allowed.
No Host/API/SSH, account/credential access, rentals, privileged operations,
reboots, external messages, commit, push or merge. Restart only this workspace's
local reviewer if needed. Keep historical attempts immutable. The handoff must
name actual closures, partial support and remaining source/owner/runtime work,
with tests and limitations. Record no human acknowledgement or acceptance.
