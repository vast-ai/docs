# Runtime/operator work — open

No live Host/API/SSH/Keychain or rental action occurred in this attempt. Earlier
evidence retains its exact scope. The prior rental was destroyed; the publicly
listed host can receive customers, so historical idle readback is not current
permission to test it.

## Next bounded qualification

Proposed target: user-owned machine **150296** only. Awaiting a reply to the new
budget request: one-GPU SSH/Jupyter run up to 20 minutes / estimated USD5;
self-test up to 30 minutes / estimated USD15; estimated aggregate USD20.
There is no established provider-enforced total-spend cap. These are requested
bounds, **not already granted authority**. No reboot, listing change, customer
workload interference or fallback to another machine.

| Exact page / occurrence | Evidence already retained and limitation | Missing prerequisite / impact and next action |
| --- | --- | --- |
| First 24 Hours / Test Like A Client; COR-01-MCL-323c8fb8180f5f62-REPLACEMENT | Canonical pinned CLI parsing recognizes the corrected machine_id / verified=any query. Current replacement UNVALIDATED. | Authorized operator retains the exact corrected client-context search, timestamp, safe account/context identity and returned machine/offer. Static syntax does not prove live visibility. Perform only after resuming the scoped live plan. |
| First 24 Hours / Test Like A Client; MCL-da591d84b7d08317 | Earlier client cleanup is independently retained. Compound occurrence remains UNVALIDATED. | New rental/spend and time-window approval plus current ownership/idle state and cleanup watchdog are needed for a suitable SSH/Jupyter check. Retain actual connection/UI and benign command output; distinguish happy path from the conditional troubleshooting clause. Do not promote the whole sentence merely because a connection works. |
| How to Self-Test / Run the Test; MCL-eeaf6da83da9eca7 | CLI signature for --support-bundle-dir exists; no corresponding complete new runtime is retained. Existing status BLOCKED. | Missing separately approved paid create/delete window, current safe idle target and pre-armed cleanup authority. Once approved, pin actual CLI revision and compatible immutable image/index/platform identity, run the exact command, retain inputs, complete diagnostic result/bundle, and independent rental absence. Initialization or cleanup alone is not diagnostic PASS. |
| Other installation, maintenance, VM, network and cross-platform workflows | Current individual proofs and limits remain unchanged. | Follow each occurrence's concrete prerequisite in the current register below. No reboot/privileged mutation, maintenance, WAN topology change, stock installer rerun or new cross-platform CI spend is added by this handoff. |

Before spending: check strict SSH identity, owned-machine/current customer
occupancy, account authorization, offer price and expiry, usable client route,
compatible command/image and narrowly scoped independent cleanup. Stop if any
guard is unavailable; do not infer Docker/host emptiness from denied access.
Use a new attempt for execution and preserve first failures.

The actual source CLI is the pinned modular entry point at ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd,
not an unrelated legacy root script. Self-test uses direct SSH and is not a
Jupyter proof. macOS local tests do not establish Linux or Windows dogfooding.

See [exact live plan](live-next-plan.md),
[full current operator worklist](../../current-runtime-operator-blockers.md),
and [prior bounded runtime handoff](../2026-09-09-h100x4-listing-rental-attempt-02/runtime-operator-register.md).
Missing proof alone stays UNVALIDATED; BLOCKED describes the specific check
prerequisite, not a blanket status assigned to every untested claim.
