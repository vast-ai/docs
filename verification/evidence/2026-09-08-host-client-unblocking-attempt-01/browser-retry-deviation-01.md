# Browser retry scheduling deviation

Retries 03–05 were launched into three distinct browser sessions but overlapped:
the command runner returned live process handles rather than waiting for each
process to exit. They must not be reported as the sequential repetitions planned
in amendment 06. They can only support their individually retained page-interface
observations. The original fetch failure remains unresolved.

After those processes finish, run the same route checks sequentially as retries
06–08, waiting for each process completion. Do not discard or relabel the
overlapping attempts. No additional Host/API operations are involved.
