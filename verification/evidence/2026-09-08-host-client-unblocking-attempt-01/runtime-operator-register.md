# Runtime/operator register — current client unblocking

This supersedes prerequisite descriptions, not the evidence, in the earlier 47-check batch register. Historical files remain unchanged. These are execution gates, separate from the Product/Finance/Legal/source-owner register.

| Item | Current disposition and evidence | Exact next action |
|---|---|---|
| Client key location and authentication | RESOLVED prerequisite. Both named keys authenticated; their stable account IDs differ. [Capture](account-observations-01.json). | No need to provide another key. Recheck credentials only if they expire or the account context changes. Authentication does not prove create permissions. |
| Client-visible offers on alternate Ada target | RESOLVED read-only observation. Client no-default search returned three selected-machine offers (1/2/4 GPUs), rentable true and rented false at capture. [Client checks](cli-execution-01.json); [Host-context searches](host-search-execution-01.json). | Repeat appropriate filters immediately before any approved run. An offer snapshot does not reserve hardware or establish billing. |
| Actual rental/self-test/workload/cleanup | BLOCKED for execution: no explicit resource-changing test authorization with agreed workload window, positive runtime limit, stop conditions and task-instance cleanup. No paid-test budget approved; self-test billing not established. | Agree scope and resource/spend bounds; establish required create/machine permissions, repeat occupancy, and track exact task-created instance for cleanup. Do not test the occupied RTX4090 or act on existing renter instances. |
| VM status command | Exact read-only invocation returned off, but kaalia.cfg is not readable to this user. [Capture](vm-status-01.json). This is NOT reliable proof of disabled VM configuration. | Operator supplies an approved status observation with sufficient read permission; never infer disabled/healthy/idle from this result or exit zero alone. |
| VM Retry Enablement complete procedure | BLOCKED: no approved post-enable representative VM-capable runtime state or permission to perform setup/restart/passthrough actions. Readable helper source and check invocation do not establish that state. | Bind upstream helper provenance; obtain explicit controlled-window mutation/cleanup authority only if the workflow is needed. |
| VM helper source readability | RESOLVED for this Ada target only. [Source excerpts](vm-helper-source-retest-02.json). Earlier H100/RTX4090 denial remains historical. | Upstream source-owner provenance is still unresolved; no claim of Git revision or released lineage. |
| GPU burn, bootloader changes, reboots, volume lifecycle/cascade tests | BLOCKED: no workload/privilege/mutation authority, protected window, recovery/cleanup scope or suitable lifecycle environment. Volume semantics also need backend source. | Resolve exact target and authority per claim; read-only evidence cannot close these. |
| Nonempty maintenance/report and Windows/packaged-Linux CLI branches | UNVALIDATED: no suitable new branch/platform evidence. | Use an approved existing sanitized nonempty fixture or designated runner. Do not manufacture live maintenance/reports or copy credentials into CI. |
| Key-storage security | Existing FAIL remains: isolated nonsecret storage fixture writes successfully but is mode 0644. [Write](local-key-storage-01.json), [limitation](local-key-storage-limitations-02.json). | Do not count successful write as secure credential storage or actual Host authentication. Keep real keys in the approved Keychain workflow; any CLI product fix needs a separately scoped change. |

Client-owned instance list was empty in this batch; no test instance was created and none required stopping/destruction. No existing instance was changed.

