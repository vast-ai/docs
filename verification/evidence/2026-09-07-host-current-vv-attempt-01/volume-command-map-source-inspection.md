# Volume Command Map CLI source inspection

## Scope and method

- **Documentation target:** `host/volume-offers.mdx` lines 101–109 at the current Docs worktree.
- **CLI source target:** `/tmp/host-vv-cli-ecf32ef`, pinned to `ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd`.
- **Method:** read-only inspection of the command decorators, request construction, and HTTP dispatch calls cited below. No CLI command, HTTP request, API, credential, marketplace, or backend execution occurred.
- **Result limit:** a cited row establishes only that this CLI revision constructs the stated client request. It does not establish backend enforcement, persistence, offer ownership, authorization, availability, lifecycle permanence, or an unattached-volume guard.

## Row-by-row source binding

| Docs row | Command and source location | Client dispatch and fields inspected | Source-only disposition |
| --- | --- | --- | --- |
| 101 — Host: publish a volume offer with the machine offer | `vastai list machine`; `vast.py:8184–8213`, shared `list_machine` at `vast.py:8133–8153` | `PUT /machines/create_asks/`; request includes `machine`, pricing fields, `vol_size`, and `vol_price` (`8136–8149`). | **UNVALIDATED** for “publish a volume offer”: client supplies volume-offer fields while listing the machine, but the client source cannot prove publication, ownership, or backend creation. |
| 102 — Host: publish one machine’s storage separately | `vastai list volume`; `vast.py:8281–8313` | `POST /volumes/`; body contains `size`, `machine`, `price_disk`, optional `end_date` (`8293–8308`). | **UNVALIDATED** for publication/availability semantics; client dispatch is present. |
| 103 — Host: publish storage on several machines | `vastai list volumes`; `vast.py:8316–8348` | `POST /volumes/`; body contains `size`, list-valued `machine`, `price_disk`, optional `end_date` (`8328–8343`). | **UNVALIDATED** for multi-machine publication/ownership semantics; client dispatch is present. |
| 104 — Host: stop advertising a volume offer | `vastai unlist volume`; `vast.py:9099–9121` | `POST /volumes/unlist`; body is `{id}` (`9107–9117`). | **UNVALIDATED** for the effect “stop advertising”; client dispatch is present, but backend state change is not inspected. |
| 105 — Renter: find volume offers | `vastai search volumes`; `vast.py:4545–4650` | `POST /volumes/search/`; default query uses `verified`, `external`, `disk_space`; request adds `order`, optional `limit`, `allocated_storage` (`4600–4641`); response reads `offers` (`4649`). | **UNVALIDATED** for what offers are actually returned or available; request construction and expected response key are client-visible. |
| 106 — Renter: create an owned volume from an offer | `vastai create volume`; `vast.py:2739–2770` | `PUT /volumes/`; body contains offer `id`, integer `size`, optional `name` (`2751–2766`). | **UNVALIDATED** for ownership and creation; the client sends an offer id but cannot establish resulting ownership or backend allocation. |
| 107 — Renter: inspect owned volumes | `vastai show volumes`; `vast.py:6526–6553` | `GET /volumes` with query `owner=me` and mapped `type`; response reads `volumes` (`6534–6553`). | **UNVALIDATED** for authorization and result completeness; client dispatch explicitly requests `owner=me`. |
| 108 — Renter: link a new or existing volume to a container | `vastai create instance`; arguments `vast.py:2437–2444`, volume validation/construction `2373–2396`, request assembly `2490–2513`, dispatch `2527–2534` | `--create-volume` or `--link-volume` requires `--mount-path`; `volume_info` has `mount_path`, `create_new`, `volume_id`, optional `name`/`size`, and is included in `PUT /asks/{id}/`. | **UNVALIDATED** for successful linking to a container; client request composition is present, but attachment, scheduling, and lifecycle semantics are backend behavior. |
| 109 — Renter: permanently remove an unattached volume | `vastai delete volume`; `vast.py:3089–3104` | `DELETE /volumes/?id=<id>` (`3097–3100`). The decorator epilog says instances using a volume “must be destroyed” (`3093–3095`), but this function contains no client-side attached-instance lookup or guard. | **UNVALIDATED** for “permanently” and “unattached.” The missing client guard is not a backend contradiction, so this is not a FAIL; only backend/API enforcement can answer whether deletion is blocked or permanent. |

## Remaining authoritative question

Direct pinned code links: [list machine](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L8133), [list volume](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L8281), [list volumes](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L8316), [unlist volume](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L9099), [search volumes](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L4545), [create volume](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L2739), [show volumes](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L6526), [instance volume request](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L2373), [delete volume](https://github.com/vast-ai/vast-cli/blob/ecf32efa1d8d2f110f7de4118c30698bb7ae2fbd/vast.py#L3089).

For each command, a backend/API owner must identify the authoritative endpoint implementation and policy/contract governing authorization, ownership, offer visibility, attachment, deletion eligibility, persistence, and lifecycle effects. For row 109 specifically, the needed proof is the endpoint’s behavior when the volume is attached and the retention/deletion semantics after success. This inspection supplies no such proof.
