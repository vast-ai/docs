# First reviewer-binding proposal — review FAIL, not published

Root inspected the isolated worker proposal before applying it. Its three focused importer tests and syntax checks were insufficient to establish reviewer integration. Five corrections were required:

1. The sixth target was a different preflight-reference row (`CUR-bfa0f979eb4ccc00`), not the requested **How to Self-Test / Run The Test** command occurrence `MCL-eeaf6da83da9eca7`. The exact requested occurrence must receive the proof and concrete blockers.
2. The page-membership check removed all new target IDs before comparison. New bindings do not change claim IDs; the complete expected ID-set check must remain intact.
3. The compound cleanup prior has the exact source-transition history decision, not the older rental decision. Its original full claim must pass the existing transition validator without an incompatible history assertion.
4. The three explicitly corrected runtime-only classifications need suitable operator roles, not the previous implementation-only role. The old role remains in the retained prior claim.
5. A next action must not ask reviewers to reuse the already destroyed instance. Any further browser run needs a newly approved idle rental and a scoped, approved trust setup.

No new model or reviewer code from this proposal was installed on port4000. The original operational evidence and all historical attempts are unchanged. The corrected proposal must pass exact-claim, source/artifact drift, live reviewer-context, passage/proof-link and offline report retests before integration is reported complete.
