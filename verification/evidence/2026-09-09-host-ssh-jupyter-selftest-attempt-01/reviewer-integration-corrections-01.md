# Reviewer integration — original failures and bounded corrections

These are repository/collector defects, not failed Vast runtime tests. Original attempts are retained unchanged. They do not override the separate SSH, Jupyter and normal-self-test observations.

| Original retained failure | Cause and correction | Independent retest / limit |
| --- | --- | --- |
| [First proposed claim binding](reviewer-binding-first-review-01.md) | Wrong self-test occurrence, weakened page-membership admission, incorrect compound history and stale owner/action wording were rejected before integration. Six exact occurrences now retain hash-pinned prior claims; full page membership is still required. | [Exact six-claim delta](claim-delta-02.json) and later strict reviewer suite. No unrelated claim or page PASS inferred. |
| [First report generation](report-generation-01.json) | Root's current-result naming correction collided with the existing read-only attempt variable. Renamed the operational pointer without relabelling historical results. | [Second generation](report-generation-02.json) succeeded. Final generated bytes are checked separately. |
| [First Python suite](python-current-final-01.json) | A private transition snapshot accidentally changed the public builder return type from dictionary to tuple. Restored `build()`'s dictionary API; the output-only helper retains the pre-connection transition. | [Second Python suite](python-current-retest-02.json) reached historical expectation checks; their remaining two failures are retained, not hidden. Final suite follows their correction. |
| [First reviewer suite](reviewer-current-final-01.json) and [first browser attempt](browser-current-final-01.json) | A registry whitespace change required a matching exact-byte pin. The new source reader incorrectly accessed `.text` rather than decoding the returned `.bytes`; strict admission failed closed. The offline six-row test also incorrectly assumed insertion order. | Current registry pinned at `0803631c77883dfb397f36fa0a07f531232edcd56616f6af600d733e1c49f63c`; byte-backed literal checks retained. [Second reviewer suite](reviewer-current-retest-02.json) passed 96/97, leaving only a stale compound-history test expectation. |
| [Second browser attempt](browser-current-retest-02.json) | The collector expected an opaque ID in visible modal text. The interface instead supplies readable page/heading/wording plus an exact `data-passage` back-link. No UI defect or runtime failure was demonstrated by that assertion. | [Third browser attempt](browser-current-retest-03.json): all 44 contexts, six exact cards, 24 contextual proof links, six offline proof dialogs, 149 citation occurrences and no remote offline resources passed. [All 88 passage controls on the two affected pages](passage-browser-01.json) also passed. |
| [Ordinary graph update](graph-update-01.json) | AST rebuild proposed unexplained node loss and refused to replace the old graph. No force override was used. | [Scoped refresh](graph-scoped-retest-02.json) preserved 8,866 unrelated nodes and 11,019 unrelated edges. Graph navigation is not claim evidence. |
| [First scoped graph attempt](graph-scoped-01.json) | Four standard-library `Path`/`Any` placeholders had no source file. Inspected and explicitly allowed only those exact extracted identities; unrelated-node/edge and source-drift checks were not relaxed. | [Scoped candidate record](graph-scoped-result-01.json) records the selected source hashes, backup and 9,121 → 9,149 node result. |

The earlier [CLI static-suite collection failure](cli-focused-static-01.json) remains distinct from the [63 narrow, self-contained unit tests](cli-focused-static-02.json). Global CLI test collection required the unavailable `Crypto` dependency. Explicit `--noconftest` scope is documented in the [scope correction](cli-focused-static-scope-01.md); this is not a full CLI test-suite PASS.

Final report links identify subsequent end-state retests. No historical attempt is deleted or silently changed into a PASS.

## Closeout corrections and final-byte gate

- The second Python suite's stale read-only prerequisite and pre-connection transition fixture were corrected. The new fixture exercises the original transition projection; **all original 2,003-record equality assertions remain intact**. An isolated worker proposed seven-ID exceptions caused by its incomplete snapshot; those exceptions were rejected. [Third Python suite](python-current-retest-03.json): 186 PASS.
- The [third reviewer suite](reviewer-current-retest-03.json) passed 96/97; only the deterministic HTML assertion failed because the last historical-wording/report-input changes had not yet been generated. [Regeneration](report-generation-03.json) supplies the corrected bytes. The [final deterministic check](html-closeout-01.json), [final full reviewer suite](reviewer-closeout-04.json), and [final full Python suite](python-closeout-04.json) record end-state outcomes separately. Earlier checks with changing source identity are not seal inputs.
- [Final browser check](browser-closeout-04.json) repeats the six contextual cards and offline proof dialogs against the final HTML. Screenshots put the reviewed card beside the highlighted customer-facing passage; the initial screenshots' sidebar positioning was improved without changing the locator or verdict.
- [Final graph record](graph-scoped-result-03.json) also includes the final test-fixture/export changes. The graph retains 9,150 nodes; 8,847 unrelated nodes and 10,998 unrelated edges are preserved in this last scoped refresh.

The final seal requires every named check to have exit zero, unchanged source identity during execution, and exact final hashes for **every** recorded source. It is not issued on the strength of an earlier green result with stale generated bytes.
