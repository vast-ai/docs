# Visual inspection and correction

Root inspected checks-01-desktop.png and checks-01-mobile.png. The report fits
the retained 1440px and 390px layouts without clipping or horizontal overflow.
The new evidence date and 241/35 counts render correctly.

One presentation defect remained: the sidebar/footer still said the presentation
was updated on 10 September. Correct those labels to 11 September and update the
exact regression assertion. The first screenshots remain retained. The final
HTML regeneration, focused browser check and deterministic export check retest
that correction; the dated Terms evidence label remains 10 September.

The initial JavaScript aggregate exposed missing current_host_jurisdiction.mjs
copies in two historical fixture builders, not a failure of the live 4000 server.
Both copy lists must include the imported module while preserving their original
historical model/registry boundaries. The original64 failed cases are retained
in javascript-full-01.json. A scoped stop was considered after diagnosis, but
the process had already exited; no process was stopped by that command.
