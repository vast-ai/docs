---
title: "VastAI.join_cluster"
sidebarTitle: "join_cluster"
---

Join machines to a cluster.

## Signature

```python
VastAI.join_cluster(
    cluster_id: int,
    machine_ids: List[int]
) -> dict
```

## Parameters

<ParamField path="cluster_id" type="int" required>
  The ID of the cluster to join.
</ParamField>

<ParamField path="machine_ids" type="List[int]" required>
  List of machine IDs to add to the cluster.
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.join_cluster(cluster_id=12345, machine_ids=[101, 102, 103])
print(result)
```
