---
title: "VastAI.accept_price_increase"
sidebarTitle: "accept_price_increase"
---

Accept the pending price increase on an instance. Raises LookupError when no pending row matches.

## Signature

```python
VastAI.accept_price_increase(
    instance_id: int
) -> dict
```

## Parameters

<ParamField path="instance_id" type="int" required>
  The ID of the instance with a pending price increase.
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.accept_price_increase(instance_id=12345)
print(result)
```
