---
title: "VastAI.accept_price_increase"
sidebarTitle: "accept_price_increase"
---

Accept a host price increase on one or more rented instances.

## Signature

```python
VastAI.accept_price_increase(
    id: Optional[int] = None,
    instance_ids: Optional[List[int]] = None,
    host_id: Optional[int] = None
) -> dict
```

## Parameters

<ParamField path="id" type="Optional[int]">
  
</ParamField>

<ParamField path="instance_ids" type="Optional[List[int]]">
  
</ParamField>

<ParamField path="host_id" type="Optional[int]">
  accept every pending price-increase from this host (mutually exclusive with IDs)
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.accept_price_increase()
print(result)
```
