---
title: "VastAI.tfa_delete"
sidebarTitle: "tfa_delete"
---

Remove a 2FA method from your account.

## Signature

```python
VastAI.tfa_delete(
    id_to_delete: Optional[int] = None,
    code: Optional[str] = None,
    sms: bool = False,
    secret: Optional[str] = None,
    backup_code: Optional[str] = None,
    method_id: Optional[str] = None
) -> str
```

## Parameters

<ParamField path="id_to_delete" type="Optional[int]">
  id_to_delete
</ParamField>

<ParamField path="code" type="Optional[str]">
  code
</ParamField>

<ParamField path="sms" type="bool" default="False">
  sms
</ParamField>

<ParamField path="secret" type="Optional[str]">
  secret
</ParamField>

<ParamField path="backup_code" type="Optional[str]">
  backup_code
</ParamField>

<ParamField path="method_id" type="Optional[str]">
  method_id
</ParamField>

## Returns

`str` — Result from the API call.

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.tfa_delete()
print(result)
```
