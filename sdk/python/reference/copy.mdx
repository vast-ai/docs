---
title: "VastAI.copy"
sidebarTitle: "copy"
---

Copy files between instances.

## Signature

```python
VastAI.copy(src: str, dst: str, identity: Optional[str] = None) -> str
```

## Parameters

<ParamField path="src" type="str" required>
  src
</ParamField>

<ParamField path="dst" type="str" required>
  dst
</ParamField>

<ParamField path="identity" type="Optional[str]">
  identity
</ParamField>

## Returns

`str` — Result from the API call.

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.copy(src="...", dst="...")
print(result)
```
