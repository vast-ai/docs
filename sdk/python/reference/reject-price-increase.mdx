---
title: "VastAI.reject_price_increase"
sidebarTitle: "reject_price_increase"
---

Reject the pending price increase on an instance. Raises LookupError when no pending row matches.

## Signature

```python
VastAI.reject_price_increase(
    instance_id: int
) -> dict
```

## Parameters

<ParamField path="instance_id" type="int" required>
  The ID of the instance with a pending price increase.
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.reject_price_increase(instance_id=12345)
print(result)
```
