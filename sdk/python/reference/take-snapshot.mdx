---
title: "VastAI.take_snapshot"
sidebarTitle: "take_snapshot"
---

Take a container snapshot and push to a registry.

## Signature

```python
VastAI.take_snapshot(
    instance_id,
    **kwargs
) -> dict
```

## Parameters

<ParamField path="instance_id" type="int" required>
  The ID of the instance to snapshot.
</ParamField>

<ParamField path="**kwargs" type="dict">
  Additional keyword arguments for snapshot configuration.
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.take_snapshot(instance_id=12345)
print(result)
```
