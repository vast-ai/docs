---
title: "VastAI.remove_machine_from_cluster"
sidebarTitle: "remove_machine_from_cluster"
---

Remove a machine from a cluster.

## Signature

```python
VastAI.remove_machine_from_cluster(
    cluster_id: int,
    machine_id: int,
    new_manager_id: Optional[int] = None
) -> dict
```

## Parameters

<ParamField path="cluster_id" type="int" required>
  The ID of the cluster.
</ParamField>

<ParamField path="machine_id" type="int" required>
  The ID of the machine to remove.
</ParamField>

<ParamField path="new_manager_id" type="int" default="None">
  If the removed machine is the manager, specify a new manager machine ID.
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.remove_machine_from_cluster(cluster_id=12345, machine_id=101)
print(result)
```
