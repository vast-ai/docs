---
title: "VastAI.create_scheduled_job"
sidebarTitle: "create_scheduled_job"
---

Create a new scheduled job.

## Signature

```python
VastAI.create_scheduled_job(
    start_time,
    end_time,
    api_endpoint,
    request_method,
    request_body,
    frequency,
    instance_id,
    **kwargs
) -> dict
```

## Parameters

<ParamField path="start_time" type="str" required>
  Start time for the scheduled job.
</ParamField>

<ParamField path="end_time" type="str" required>
  End time for the scheduled job.
</ParamField>

<ParamField path="api_endpoint" type="str" required>
  API endpoint to call when the job runs.
</ParamField>

<ParamField path="request_method" type="str" required>
  HTTP method for the request (e.g., "GET", "POST").
</ParamField>

<ParamField path="request_body" type="str" required>
  Body of the request to send.
</ParamField>

<ParamField path="frequency" type="str" required>
  How often the job should run.
</ParamField>

<ParamField path="instance_id" type="int" required>
  The instance ID to associate with the job.
</ParamField>

<ParamField path="**kwargs" type="dict">
  Additional keyword arguments for job configuration.
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.create_scheduled_job(
    start_time="2024-01-01T00:00:00Z",
    end_time="2024-12-31T23:59:59Z",
    api_endpoint="/api/v0/instances/12345/start",
    request_method="PUT",
    request_body="{}",
    frequency="daily",
    instance_id=12345
)
print(result)
```
