---
title: "VastAI.detach_ssh"
sidebarTitle: "detach_ssh"
---

Detach an SSH key from an instance.

## Signature

```python
VastAI.detach_ssh(
    instance_id: int,
    ssh_key_id: str
) -> dict
```

## Parameters

<ParamField path="instance_id" type="int" required>
  id of the instance
</ParamField>

<ParamField path="ssh_key_id" type="str" required>
  id of the key to detach from the instance
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.detach_ssh(instance_id=12345, ssh_key_id="value")
print(result)
```
