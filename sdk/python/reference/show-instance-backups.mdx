---
title: "VastAI.show_instance_backups"
sidebarTitle: "show_instance_backups"
---

List the backups Vast has taken of your instances and volumes.

## Signature

```python
VastAI.show_instance_backups(
    contract_id: Optional[int] = None
) -> list[dict]
```

## Parameters

<ParamField path="contract_id" type="Optional[int]">
  only show backups of this instance or volume contract id
</ParamField>

## Returns

`list[dict]`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.show_instance_backups()
print(result)
```
