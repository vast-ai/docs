---
title: "VastAI.attach_ssh"
sidebarTitle: "attach_ssh"
---

Attach an SSH key to an instance.

## Signature

```python
VastAI.attach_ssh(
    instance_id: int,
    ssh_key: str
) -> dict
```

## Parameters

<ParamField path="instance_id" type="int" required>
  id of instance to attach to
</ParamField>

<ParamField path="ssh_key" type="str" required>
  ssh key to attach to instance
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.attach_ssh(instance_id=12345, ssh_key="value")
print(result)
```
