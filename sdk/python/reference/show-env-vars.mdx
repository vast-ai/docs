---
title: "VastAI.show_env_vars"
sidebarTitle: "show_env_vars"
---

Show user environment variables.

## Signature

```python
VastAI.show_env_vars(
    show_values: bool = False
) -> dict
```

## Parameters

<ParamField path="show_values" type="bool">
  Show the values of environment variables
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.show_env_vars()
print(result)
```
