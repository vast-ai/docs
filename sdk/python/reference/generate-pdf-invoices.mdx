---
title: "VastAI.generate_pdf_invoices"
sidebarTitle: "generate_pdf_invoices"
---

Generate PDF invoices based on filters.

<Warning>This method is **not yet implemented** in the SDK and will raise `NotImplementedError` when called. Use the CLI `vastai generate pdf_invoices` instead.</Warning>

## Signature

```python
VastAI.generate_pdf_invoices(**kwargs)
```

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
# Not yet implemented — raises NotImplementedError
# result = client.generate_pdf_invoices()
```
