---
title: "VastAI.tfa_send_sms"
sidebarTitle: "tfa_send_sms"
---

Request a 2FA SMS verification code.

## Signature

```python
VastAI.tfa_send_sms(
    **kwargs
) -> dict
```

## Parameters

<ParamField path="**kwargs" type="dict">
  Keyword arguments including phone number or method details.
</ParamField>

## Returns

`dict`

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.tfa_send_sms(phone="+1234567890")
print(result)
```
