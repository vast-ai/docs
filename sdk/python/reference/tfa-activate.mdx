---
title: "VastAI.tfa_activate"
sidebarTitle: "tfa_activate"
---

Activate a new 2FA method by verifying the code.

## Signature

```python
VastAI.tfa_activate(
    code: str,
    sms: bool = False,
    secret: Optional[str] = None,
    phone_number: Optional[str] = None,
    label: Optional[str] = None
) -> str
```

## Parameters

<ParamField path="code" type="str" required>
  The 2FA verification code from your authenticator app or SMS.
</ParamField>

<ParamField path="sms" type="bool" default="False">
  Use SMS delivery for the 2FA code instead of an authenticator app.
</ParamField>

<ParamField path="secret" type="Optional[str]">
  Secret token from the previous login step (required for SMS or Email 2FA).
</ParamField>

<ParamField path="phone_number" type="Optional[str]">
  Phone number for SMS-based 2FA activation.
</ParamField>

<ParamField path="label" type="Optional[str]">
  Human-readable label for this 2FA method.
</ParamField>

## Returns

`str` — Result from the API call.

## Example

```python
from vastai import VastAI

client = VastAI(api_key="YOUR_API_KEY")
result = client.tfa_activate(code="...")
print(result)
```
